# Repository contents

This repository contains the Jupyter notebooks supporting the MSc thesis.

## Note on duplicate files

Several entries appear twice in the file listing, with similar names but different upload dates. **When a duplicate exists, the valid file is the one whose name ends with the suffix `_1_1`**  this is the latest revision and the only one that should be used.

The earlier copy is retained for traceability only and should be ignored.
